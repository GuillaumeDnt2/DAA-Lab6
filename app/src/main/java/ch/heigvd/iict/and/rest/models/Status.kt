package ch.heigvd.iict.and.rest.models

/**
 * Enum used to tell the state of the contacts in the database
 *
 * Authors : Dunant Guillaume, Junod Arthur, Häffner Edwin
 */
enum class Status {
    OK, NEW, MOD, DEL
}